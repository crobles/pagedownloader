const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');

chai.use(chaiAsPromised);

describe('db/models/object:', () => {

  before(function() {
    // apply population data
  });

  after(function() {
    // clear data
  });

  describe('read():', () => {
    it('Should find a object', async () => {
      // call mathods
    });
  });

  describe('update():', () => {
    it('Should update one or more documents', async () => {
      // call mathods
    });
  });

});
